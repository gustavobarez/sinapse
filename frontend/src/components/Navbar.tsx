export function Navbar() {
  return (
    <div className="flex items-center gap-2 p-5 ">
      <div className="text-lg font-semibold bg-gradient-to-r from-[hsl(var(--gradient-start))] to-[hsl(var(--gradient-end))] bg-clip-text text-transparent">
        Sinapse
      </div>
    </div>
  );
}
